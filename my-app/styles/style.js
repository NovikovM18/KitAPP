import { StyleSheet } from "react-native";

export const gStyle = StyleSheet.create({
  main: {
    padding: 15,
    backgroundColor: '#ccc',
    borderRadius: '75',
  },
  title: {
    padding: 15,
    backgroundColor: '#ccc',
    // borderRadius: '25%',
    fontSize: 36,
    color: '#666',
    fontWeight: '900',
    textAlign: 'center',
    marginBottom: 16,
  }
})